version: 2

models:
    - name: accounts
      description: "A starter dbt model"
      columns:
          - name: Id
            description: "The primary key for this table"
            tests:
                - unique
              #  - not_null
          - name: Name

    #- name: my_second_dbt_model
    #  description: "A starter dbt model"
    #  columns:
    #      - name: id
    #        description: "The primary key for this table"
    #        tests:
    #            - unique
    #            - not_null
